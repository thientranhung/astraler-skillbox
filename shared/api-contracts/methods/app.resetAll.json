{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AppResetAllMethod",
  "description": "Contract for app.resetAll JSON-RPC method. Deletes the SQLite database and signals the app to restart. Irreversible — all Skillbox data is lost.",
  "oneOf": [
    { "$ref": "#/definitions/AppResetAllRequest" },
    { "$ref": "#/definitions/AppResetAllResponse" }
  ],
  "definitions": {
    "AppResetAllRequest": {
      "title": "AppResetAllRequest",
      "description": "No params required for app.resetAll.",
      "type": "object",
      "properties": {},
      "additionalProperties": false
    },
    "AppResetAllResponse": {
      "title": "AppResetAllResponse",
      "type": "object",
      "properties": {
        "restarting": {
          "type": "boolean",
          "description": "Always true on success. Electron main triggers app.relaunch() upon receiving this response."
        }
      },
      "required": ["restarting"],
      "additionalProperties": false
    }
  }
}
