{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ProviderSetEnabledMethod",
  "description": "Contract for provider.setEnabled JSON-RPC method. Persists the user's enabled/disabled preference for a provider.",
  "oneOf": [
    { "$ref": "#/definitions/ProviderSetEnabledRequest" },
    { "$ref": "#/definitions/ProviderSetEnabledResponse" }
  ],
  "definitions": {
    "ProviderSetEnabledRequest": {
      "title": "ProviderSetEnabledRequest",
      "description": "Params for provider.setEnabled.",
      "type": "object",
      "properties": {
        "providerKey": {
          "type": "string",
          "description": "Stable provider key (e.g. generic_agents, claude)"
        },
        "enabled": {
          "type": "boolean",
          "description": "Whether to enable or disable the provider"
        }
      },
      "required": ["providerKey", "enabled"],
      "additionalProperties": false
    },
    "ProviderSetEnabledResponse": {
      "title": "ProviderSetEnabledResponse",
      "description": "Confirmation that the provider enablement preference was saved. Errors: validation_error (1002) if provider unknown or enabled=true for a canToggle=false provider; database_error (1004) if DB unavailable.",
      "type": "object",
      "properties": {
        "updated": {
          "type": "boolean",
          "description": "Always true; indicates the preference was persisted successfully"
        }
      },
      "required": ["updated"],
      "additionalProperties": false
    }
  }
}
