# Multi Provider Settings Fixture

Template for provider enablement isolation cases. Use a QA DB with at least two
provider definitions enabled, then toggle only one provider through Settings.
No real provider folders are needed.
