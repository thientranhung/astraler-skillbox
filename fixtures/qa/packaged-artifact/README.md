# Packaged Artifact Fixture

Marker template for packaged smoke cases. The real app artifact is not tracked
in Git. A QA run must record the approved artifact path, smoke app-data path, and
forbidden repo/dev DB paths in its run-local copy before executing packaged
cases.
