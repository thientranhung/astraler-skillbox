# Project With Provider Override

Template for provider path override reset cases. Both paths are present and
distinct so QA can prove resetting an override changes metadata without deleting,
moving, or rewriting filesystem content.
