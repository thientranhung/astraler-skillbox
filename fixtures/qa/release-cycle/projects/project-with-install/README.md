# Project With Install

Template for cases that need a pre-existing managed install. The QA run should
copy this folder, then create the managed target inside `.agents/skills` against
the run-local copied host. Do not point the tracked fixture at a real host path.
